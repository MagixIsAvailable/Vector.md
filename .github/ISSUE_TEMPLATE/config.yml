blank_issues_enabled: true
contact_links:
  - name: wire-pod (upstream firmware/voice pipeline)
    url: https://github.com/kercre123/wire-pod
    about: If your issue is actually about wire-pod itself (flashing, pairing, the voice/intent pipeline) rather than this repo's Python layer, it likely belongs upstream instead.
